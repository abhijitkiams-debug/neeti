# Committed on purpose — no real secrets here (see .gitignore comment).
# For a real deployment, override JWT_SECRET (and point DATABASE_URL at
# Postgres) via your hosting platform's environment variables instead of
# editing this file.
DATABASE_URL="file:./dev.db"
JWT_SECRET="dev-only-insecure-secret-change-me"
