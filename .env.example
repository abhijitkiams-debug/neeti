DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-to-a-long-random-string-in-production"

# Optional. Set a real key to enable "Generate with AI" on the micro-quiz
# question bank (src/lib/ai.ts). Without it, the button returns a clear
# "not configured" message instead of failing silently.
ANTHROPIC_API_KEY=""
